React + MUI + Redux sample app created by ChatGPT.

How to run:
1. npm install
2. npm start

This project demonstrates:
- Material UI theming (ThemeProvider)
- Redux Toolkit store + slice
- Simple API integration (mocked)
- Components broken into small subcomponents
